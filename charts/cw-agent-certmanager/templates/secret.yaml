{{- if include "cw-agent-certmanager.createSecret" . }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "cw-agent-certmanager.fullname" . }}-api-key
  labels:
    {{- include "cw-agent-certmanager.labels" . | nindent 4 }}
  annotations:
    "helm.sh/resource-policy": keep
type: Opaque
data:
  api-key: {{ include "cw-agent-certmanager.apiKeyValue" . | b64enc | quote }}
{{- end }}
